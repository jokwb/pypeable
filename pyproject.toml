[project]
name = "pypeable"
version = "0.1.0"
requires-python = ">=3.14"

[tool.hatch.build.targets.wheel]
packages = ["src/pypeable"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"